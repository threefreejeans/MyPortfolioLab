# server/.env (example)
NODE_ENV=development
PORT=3000

# MongoDB (use a connection string without embedding in code)
MONGO_URI=mongodb://localhost:27017/mernproject

# JWT
JWT_SECRET=some_long_random_secret_here
JWT_EXPIRES_IN=1d